# SD-008 Azure Staging Local Implementation Evidence

**Status:** IMPLEMENTED locally; hosted SD-008 proof BLOCKED at separately required mutation gates

**Date:** August 20, 2026

**Branch:** `work/sd-008-azure-staging-baseline`

**Base and current HEAD:** `main` at `623ff665d2a276c7541622f73e34d15ee6a7d2bf`

**Ship target:** future reviewable pull request after a separate Git mutation approval

## Bounded claim

The ratified ADR, application health contracts, deterministic release identity, non-root container
source, database-role bootstrap, migration safety check, private Azure Bicep graph, consequence-gated
GitHub workflow, evidence export, rollback path, teardown path, and non-mutating learning boundary
are implemented and locally checked. No Git staging, commit, push, pull request, GitHub Environment,
package, identity, Azure resource, deployment, traffic, rollback, teardown, or learning mutation was
performed.

This record does not claim a built container, public GHCR artifact, Azure staging environment,
hosted migration, authenticated ingress, hosted rollback, hosted teardown, completed learning
packet, L2, L3, production readiness, or deployment.

## Live reread after implementation

- repository: `https://github.com/Neel-Error404/signal-desk`, `PUBLIC`, default `main`;
- branch and HEAD: `work/sd-008-azure-staging-baseline` at exact protected-main commit
  `623ff665d2a276c7541622f73e34d15ee6a7d2bf`;
- remote SD-008 branch: absent;
- active ruleset: `21058424` / `signaldesk-hosted-review-gate`;
- ruleset targets: `main` and `work/sd-006-release-communication`;
- required check: strict `signaldesk-ordered-review-gate`;
- bypass actors: zero; current user bypass: `never`;
- GitHub Environments: zero;
- GitHub deployments: zero;
- GHCR package `Neel-Error404/signaldesk`: absent;
- Azure resource group `rg-signaldesk-stg-cin`: absent;
- PR #4: closed without merge and not modified;
- PR #6: already merged before this slice and not modified;
- Git index: empty; all SD-008 implementation remains unstaged.

## Implemented local surfaces

1. ADR 0009 is ratified for bounded local implementation and preserves all later mutation gates.
2. The machine-readable deployment contract binds exact repository/base identity, four consequence
   Environments, three Azure service principals, digest-only deployment, private data planes,
   separate database identities, health, rollback, teardown, and learning authority.
3. Health endpoints expose bounded release prefixes and correlation identity. Readiness performs a
   two-second PostgreSQL probe and returns a redacted explicit `503` on failure.
4. Next.js standalone output uses the exact Git commit as hosted build ID.
5. The Dockerfile uses Node `22.18.0-bookworm-slim` pinned to the resolved multi-architecture digest,
   prunes development dependencies, runs as `node`, and retains the Prisma CLI and role-bootstrap
   script so application, bootstrap, and migration jobs use one digest.
6. Database bootstrap creates `signaldesk_migration` and `signaldesk_runtime`, grants schema-change
   authority only to migration, grants ordinary product DML only to runtime, and emits no secret.
7. Bicep composes a delegated VNet, private PostgreSQL 16 `Standard_B1ms`, run-unique
   purge-protected Key Vault, three user-assigned identities, secret-scoped grants, Log Analytics,
   Container Apps jobs, exact-digest application, probes, Entra authentication, and multiple
   revisions.
8. The manual workflow reruns the product gate, proves two normalized OCI builds, scans image
   contents and vulnerabilities, emits and attests an SBOM, pushes/attests one digest, pauses for
   public visibility, provisions only after OIDC approval, runs bootstrap then migrations, creates a
   zero-traffic candidate, requires authenticated health and owner flow, moves staging traffic,
   restores the baseline, exports redacted logs/evidence, and deletes the exact resource group.
9. Publication, provision, traffic, and teardown are four independent human decisions. Provision,
   traffic, and teardown use different Entra service principals so provider permissions do not
   aggregate behind multiple GitHub subjects on one principal.

## Elder capsule review

- activation: `activation-signaldesk-f119f189c60b698d`;
- activation digest: `c5a31bfeeb2ce2723dad36b0ca063f1d659765a3de5dc679786da2430f307a90`;
- activation state: ready, L2 ceiling, zero trusted memory records;
- project-owned intake source: `config/elder-intake-azure-staging.json`;
- generated candidate location: `.tmp/sd008-capsule-candidate`;
- generated files: 67;
- candidate differences from the installed capsule: seven generated files, limited to profile,
  contract/authority digests, resolved obligations, graph/profile records, and `ARCHITECTURE.md`;
- installed generated capsule: unchanged because replacement requires the separate Git approval
  and generated-diff review gate.

## Ordered local verification

| Level | Result |
|---|---|
| Foundation | PASS; lockfile, Prisma generation, types, lint, schema, six-migration safety, 46 contracts, layout, and dependency boundaries |
| Component | PASS; 50 tests including metadata parsing, liveness, readiness success, bounded timeout, redacted failure, and explicit `503` |
| Integration | PASS; role bootstrap, six migrations as migration identity, 25 tests as runtime identity, and test-admin-only immutable fixture cleanup |
| Workflow | PASS; desktop and mobile Chrome complete owner flows |
| Stress | PASS; 19 bounded concurrency, outage, recovery, redaction, and cleanup checks |
| Build | PASS; exact commit-bound Next.js standalone build with both health routes |
| Dependency audit | PASS; zero vulnerabilities at high threshold |
| Bicep | PASS; `infra/staging/main.bicep` compiles with Bicep 0.46.1 and no diagnostics |
| GitHub workflow lint | PASS; actionlint 1.7.12 accepts both workflows |
| Elder validation | PASS |

Integration initially failed because the legacy fixture used `TRUNCATE` through the newly
restricted runtime credential. Replacing it with ordinary `DELETE` then correctly hit immutable
table triggers. The root fix was a separate test-admin connection used only for isolated fixture
reset; every product operation and readiness check remains on the restricted runtime role. The
same Integration level was rerun and passed.

Git-delivery revalidation found and removed one actual Azure subscription UUID from the public
deployment contract before staging. The contract now binds the value symbolically to the protected
GitHub Environment variable `AZURE_SUBSCRIPTION_ID`; the same Foundation and privacy level was
rerun and passed. The first subsequent Integration invocation exceeded the local command bound and
left only its verified local test processes. Those exact processes were stopped, no source change
was made, and the same Integration level then completed with 25 passing tests and no remaining
recent PostgreSQL worker. The first Stress invocation exited from the local Node runner with an
uncaught undefined value before returning a test verdict. A traced same-level rerun made no source
change and passed all 19 checks. These transient local runner events do not weaken the requirement
for the hosted ordered gate on the pull request.

## Exact implementation digests

- ADR 0009 SHA-256: `4eb83a8dd0bd94608b3e3cba1a6efd3e7dd70f859217c3363a6e8d459bf402de`;
- deployment contract SHA-256: `ef4438b6d586d243c1dc40e0857e5498daf2b96ca31296d4e1179db1d1925dc8`;
- Dockerfile SHA-256: `998087ddcd0f4d9f2f58074f12d5c3e8b97303b5701ffea9570069de788ee933`;
- staging workflow SHA-256: `dc69c26a1c4d763fc20c601689509d9d99111215a04d48ba532dccba9c882522`;
- main Bicep SHA-256: `23196c68169e17971612678ea63af887236b138569311ab277e31b289326d609`;
- health service SHA-256: `01785899e575f308fea65103d40d5102b27a8e46304a0c86620fef5315986995`;
- database bootstrap SHA-256: `abd7a3230141c7d73d1e9c776fea9c999ef82f21c6aeecce08f5af95f49b2924`.

These are working-tree evidence digests, not Git object IDs. They must be recomputed after any
review amendment before a future commit or hosted run.

## Local limitation and hosted blockers

- No Docker, Podman, or nerdctl runtime exists on this workstation. Container build, execution,
  application-tree comparison, OCI-manifest comparison, SBOM, vulnerability scan, and image leak
  scan are implemented in the hosted workflow but are not locally verified.
- No authority exists yet to stage, commit, push, or open the implementation pull request.
- The four GitHub Environments, three OIDC principals, Entra ingress application, conditional Azure
  roles, USD 5 budget alert, and per-session tagged resource group do not exist.
- The public GHCR visibility mutation has not occurred.
- Azure quota, live price, provider registration, Central India SKU, and live Bicep `what-if` remain
  unverified.
- Therefore no actual hosted SD-008 trace exists.

## Learning lane

Repository-dreaming policy permits only candidate output, but the required learning input is the
actual redacted hosted SD-008 trace. That trace does not exist. The earlier `.env` check used the
wrong configuration path. A corrected live check through `.elder/runtime/memory.env` on August 20,
2026 passed with activation `activation-signaldesk-2e6be5f31fb2576e`, activation digest
`5ab5e423b234fca6ac61cfe1edb82ff5c7e2e7d13fa3982d8753dcf7a658f912`, candidate-only dreaming,
and zero trusted memory records. The learning engine is therefore ready; the hosted trace remains
the blocker.

No candidate was generated, accepted, evaluated, approved, rejected, promoted, or applied. After
hosted teardown, the learning lane may produce at most one quarantined candidate, must use an
independent evaluator, must obtain an exact human approval or rejection bound to candidate and
evaluation digests, and may emit only a non-mutating promotion packet. The candidate must express a
provider-neutral cloud authority lesson. Azure service names may identify the observed adapter but
must not become the universal rule.

## Verdict

The bounded local implementation is `IMPLEMENTED`. Full SD-008 and its learning packet are
`BLOCKED` at explicitly reserved Git/GitHub/Azure/hosted-trace approvals and capabilities. This is
not an implementation failure and does not weaken the stop boundary.

# SD-007 Hosted Review Gate Evidence

**Status:** IMPLEMENTED LOCALLY; HOSTED PROOF PENDING

**Date:** August 19, 2026

**Branch:** `work/sd-007-hosted-review-gate`

**Base:** `work/sd-006-release-communication` at
`5879dc0c7285ecdddddcf23b1c4ea632db21b15c`

**Ship target:** `reviewable-pr`

## Bounded claim

SD-007 is complete only when a real stacked pull request runs the exact product-owned ordered
ladder and an active GitHub rule requires the observed check. A controlled failing revision must
be blocked and the corrected exact revision must be green. Hosted CI and protected Git delivery
are not application deployment and do not establish canonical L2.

## Preflight evidence

- Repository: `https://github.com/Neel-Error404/signal-desk`, private at preflight, owner viewer
  permission `ADMIN`, default branch `main`.
- PR #4: open, non-draft, mergeable, no hosted checks; left unmodified and unmerged.
- GitHub ruleset and branch-protection APIs: HTTP 403 under the private GitHub Free state.
- Privacy scan: all seven remote branches, sixteen remote commits, thirteen trees, and 332 unique
  historical/current blobs reconciled with the local history scan; no usable credential, private
  customer record, runtime database, deployment artifact, or generated product state found.
- GitHub Actions inventory: no workflow, secret, variable, artifact, release, deployment, or
  environment existed at preflight.
- Public-disclosure decision: owner approved public visibility after reviewing code/history/PR,
  email-metadata, forking, and no-license consequences.

## Live local diagnosis

| Level | Result |
|---|---|
| Foundation wrapper | TIMEOUT at the diagnostic command bound; no test defect reported |
| Foundation corrected rerun | PASS; 38 tests plus lockfile, generated client, types, lint, schema, layout, and boundaries |
| Component | PASS; 41 tests |
| Integration | PASS; 24 tests against embedded PostgreSQL with six migrations |
| Workflow diagnosis | FAIL after both Chrome flows passed; shared PostgreSQL cleanup returned Windows `EBUSY` |
| Workflow corrected rerun | PASS; desktop and mobile Chrome flows plus bounded cleanup |
| Stress corrected rerun | PASS; 19 product, concurrency, outage, recovery, redaction, and cleanup checks |
| Build | PASS; optimized application routes compiled |
| Dependency audit | PASS; 0 vulnerabilities |
| Elder validation | PASS |

The stopped PostgreSQL PID was absent after failure. The same `EBUSY` then reproduced after a
successful Workflow run, proving that the shared Windows PostgreSQL cleanup path was the root
cause. The bounded correction uses Node's native recursive-removal retry in both the shared
runtime helper and the Stress-specific persistent cleanup while retaining an explicit error when
the retry bound is exhausted. A second Workflow diagnosis found a Windows process-exit race:
the child had emitted `exit`, but the subsequent best-effort `taskkill` lookup returned a stale
nonzero code. The correction treats the observed child `exit` as authoritative while still
failing if the child does not exit within the bound or if descendant locks prevent bounded
cleanup. Workflow was rerun at the same failed level before Stress, Build, Audit, and Elder
validation advanced. The final exact local ladder is rerun before commit.

The final ladder also exposed a cold-compile timing bound: the first desktop Workflow reached the
complete release-communication state but exhausted Playwright's implicit 30-second whole-test
timeout; the warm mobile path passed in 9.1 seconds. The explicit whole-test bound is now 60
seconds with retries still disabled, so a slow hosted cold start remains bounded and a real failure
is not hidden by retry.

## Pending immutable provider evidence

- SD-007 pull-request identity and trusted URL;
- exact base, head, commit, tree, and diff;
- workflow, run, job, check, actor, event, and timestamps;
- public-visibility transition and active ruleset identity/configuration;
- controlled failing revision, failed check, and blocked merge state;
- corrected revision, green check, and protected merge state;
- actual-versus-planned trace and all human interventions;
- quarantined Prime candidate, observations, independent evaluation, exact human decision,
  replay, and non-mutating promotion packet when approved.

## First private hosted run

- Workflow run: `32288199733`.
- Job: `96182652716` / `signaldesk-ordered-review-gate`.
- Exact revision: `53e7fa3b23acdc622aa0544ffeba932675cf4e22`.
- Setup, deterministic install, Foundation, and Component passed.
- Integration failed before product tests because PostgreSQL `initdb` could not change directory
  permissions under GitHub's checked-out `D:/a/...` workspace.
- Workflow through Elder validation correctly did not run after the failed level.
- Root cause: the embedded PostgreSQL data directory was coupled to the repository workspace,
  while the hosted Windows runner requires mutable runtime state under `runner.temp`.
- Correction: accept one explicit absolute `SIGNALDESK_TEST_RUNTIME_ROOT`, retain the local
  `.elder/runtime` default, and bind the hosted runtime to GitHub's runner temporary directory.
  The ordered product commands remain unchanged.
- GitHub also emitted an advisory warning that the pinned Action revisions use a Node.js 20
  action runtime that GitHub currently forces onto Node.js 24. The warning did not fail the job
  and grants no reason to change the product's explicitly selected Node.js 22 toolchain.

## Second private hosted run

- Workflow run: `32291195856`, exact head
  `1ede9693e4f27066363daa9813a6430ac8fd5579`.
- GitHub created no job and no check run; the provider classified the rejected workflow
  evaluation as a failed `push` event.
- Root cause: the `runner` expression context is not available in job-level `env`, so the job was
  rejected before a pull-request runner could start.
- Correction: derive the path from GitHub's trusted `RUNNER_TEMP` default environment variable in
  a PowerShell setup step and publish the absolute value through `GITHUB_ENV`. The eight ordered
  product commands remain unchanged.

## Third private hosted run

- Workflow run: `32291491906`; job/check `96193105145` /
  `signaldesk-ordered-review-gate`; exact head
  `502f1e16214a43cd5210701c143c7748725c8f06`.
- Checkout, runtime setup, pinned tool setup, deterministic install, Foundation (38), and
  Component (41) passed. Later levels were skipped after Integration failed.
- Integration again failed before product tests because `initdb` could not change permissions,
  now under the verified `D:/a/_temp/signaldesk-runtime` path.
- Root cause: GitHub's `RUNNER_TEMP` is on the same hosted `D:/a` work volume whose ACL prevents
  this embedded PostgreSQL `initdb` permission operation.
- Correction: use the hosted runner user's `LOCALAPPDATA` location on `C:` and fail explicitly if
  it is unavailable. The repository, ordered product commands, and no-secret boundary are
  unchanged.

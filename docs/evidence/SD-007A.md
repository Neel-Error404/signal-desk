# SD-007A Mainline Hosted Review Gate Evidence

**Status:** VERIFIED

**Date:** August 20, 2026

**Branch:** `work/sd-007a-mainline-gate`

**Base:** `main` at `5a7b43aa29e50e8eae0e91938a8aa747f19b9177`

**Ship target:** `reviewable-pr`

## Bounded claim

SD-007A closes one integration gap: the already protected exact check must also be emitted for a
real pull request whose base is `main`. The correction must not change the check identity, ordered
commands, runner, permissions, pins, product behavior, or GitHub ruleset, and the proof pull
request must remain open and unmerged.

## Live preflight

- Repository: `https://github.com/Neel-Error404/signal-desk`, `PUBLIC`, viewer permission
  `ADMIN`, default branch `main`.
- Main: `5a7b43aa29e50e8eae0e91938a8aa747f19b9177`.
- SD-007 source: `27f2d2bd8ce7369adb1b13fc5f89b5bdd5421a3b`, full corrected hosted gate
  green on PR #5.
- PR #4 and PR #5: open, non-draft, mergeable, and left unmodified and unmerged.
- Active ruleset: `21058424` / `signaldesk-hosted-review-gate`.
- Ruleset targets: `refs/heads/main` and
  `refs/heads/work/sd-006-release-communication`, no exclusions.
- Required check: strict `signaldesk-ordered-review-gate`; no bypass actors;
  `current_user_can_bypass: never`.
- Worktree: clean before creating `work/sd-007a-mainline-gate` from exact SD-007 head.
- Git identity: `Neel <neelabhasamadder@gamil.com>`.

## Reproduced integration gap

The live ruleset required `signaldesk-ordered-review-gate` on `main`, but the workflow at the
verified SD-007 head subscribed only to pull requests targeting
`work/sd-006-release-communication`. GitHub would therefore fail closed on a main-targeting pull
request because the required check could never be emitted. This did not invalidate PR #5's exact
stacked proof, but it prevented a usable cumulative protected-delivery path to `main`.

## Correction

The workflow base filter now includes both `main` and
`work/sd-006-release-communication`. No other workflow behavior or protected-delivery setting is
changed. A Foundation contract binds both exact base branches and the separate SD-007A work item.

## Local ordered verification

| Level | Result |
|---|---|
| Foundation | PASS; 40 contracts plus lockfile, generated client, types, lint, schema, layout, and boundaries |
| Component | PASS; 41 tests |
| Integration | PASS; 24 tests against embedded PostgreSQL with six migrations |
| Workflow | PASS; desktop and mobile Chrome owner flows plus bounded cleanup |
| Stress | PASS; 19 concurrency, outage, recovery, redaction, and cleanup checks |
| Build | PASS; optimized application routes compiled |
| Dependency audit | PASS; 0 vulnerabilities |
| Elder validation | PASS |

## Hosted mainline proof

- Pull request: `https://github.com/Neel-Error404/signal-desk/pull/6`, cumulative base `main`,
  open, non-draft, mergeable, and unmerged.
- Exact base: `5a7b43aa29e50e8eae0e91938a8aa747f19b9177`, tree
  `a971921fc2f1191502d9ab67387e2b04ee6d15f6`.
- Proof head: `558d6f054daedb3ac4d626e54abddb9d4dd2ac50`, tree
  `4a8e5db77b0b6014e8b66c33c2cef74fa599e4fa`.
- Cumulative binary diff object SHA-1 from the declared base to the proof head:
  `3658151cc8b33e2769d370708dcd3ecb69be540c`.
- Workflow: `338042633` / `SignalDesk Hosted Review Gate`.
- Pull-request run: `32304072102`, attempt 1, actor and triggering actor `Neel-Error404`.
- Job/check: `96232907316` / `signaldesk-ordered-review-gate`.
- Started: `2026-08-19T21:28:51Z`; completed: `2026-08-19T21:31:06Z`; result
  `success` in 2 minutes 15 seconds.
- Exact hosted order passed: deterministic install, Foundation, Component, Integration, Workflow,
  Stress, Build, dependency audit, and Elder validation. Setup and post-job controls also passed.
- While the exact check was pending, GitHub reported the pull request `BLOCKED`. After success it
  reported `CLEAN`, proving both enforcement and satisfaction on a real `main`-targeting pull
  request.
- GitHub emitted the previously recorded advisory that pinned Node.js 20 Action implementations
  are forced onto the provider's Node.js 24 Action runtime. The application toolchain remained
  pinned to Node.js 22.18.0 and Python 3.13.5, and the advisory did not weaken or fail the gate.

## Protected-delivery reread

- Active ruleset: `21058424` / `signaldesk-hosted-review-gate`.
- Exact targets remain `refs/heads/main` and
  `refs/heads/work/sd-006-release-communication`, with no exclusions.
- Rules remain pull request required, strict required check
  `signaldesk-ordered-review-gate`, deletion prohibited, and non-fast-forward updates prohibited.
- Bypass actors remain empty and `current_user_can_bypass: never`.
- No repository visibility, ruleset, secret, environment, deployment, release, or branch mutation
  was needed after the approved branch push and pull-request creation.

## Evidence-finalization rule

This evidence and the bounded handoff are a documentation-only follow-up to the proven
implementation head. Their exact successor revision must also pass the same required hosted check
before the slice stops. The final live PR check and immutable PR comment bind that successor
revision without creating an infinite sequence of commits that try to record their own run IDs.

## Stop boundary

PR #4, PR #5, cumulative PR #6, and Elder PR #6 remain open and unmerged. No staging,
production, deployment, release, rollback, SD-008, P5.2 activation, canonical L2, L3, canary, or
learning-candidate application occurred. SD-007A establishes a protected cumulative review path
to `main`; it does not exercise that path by merging.

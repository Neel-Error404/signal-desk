# SD-007A Mainline Hosted Review Gate Verification Plan

## Claim under test

The unchanged SignalDesk hosted review gate runs when a real pull request targets `main`, and the
active no-bypass ruleset requires its exact `signaldesk-ordered-review-gate` check before merge.
This is a cumulative mainline integration proof, not a merge, deployment, release, or maturity
promotion.

## Reproduction

At exact SD-007 head `27f2d2bd8ce7369adb1b13fc5f89b5bdd5421a3b`, the active ruleset targeted
both `main` and `work/sd-006-release-communication`, but the workflow's `pull_request.branches`
filter contained only `work/sd-006-release-communication`. A pull request targeting `main` would
therefore be required to report a check that the workflow could not create.

## Bounded correction

Add only `main` to the workflow's base-branch filter. Retain the exact check name, one sequential
Ubuntu 24.04 job, read-only contents permission, immutable action revisions, no secrets, checkout
credential isolation, and all eight product commands in their existing order.

## Verification order

1. `npm run test:foundation`
2. `npm run test:component`
3. `npm run test:integration`
4. `npm run test:workflow`
5. `npm run test:stress`
6. `npm run build`
7. `npm audit --audit-level=high`
8. `.\elder.ps1 validate`

Stop at the first failed level, correct its root cause, and rerun that same level before advancing.

## Hosted proof

1. Push `work/sd-007a-mainline-gate` from the exact verified SD-007 head.
2. Open one cumulative pull request to exact `main` base
   `5a7b43aa29e50e8eae0e91938a8aa747f19b9177`.
3. Verify GitHub creates the exact required check and the unchanged ladder passes.
4. Reread the active ruleset, base/head objects, check result, and pull-request merge state.
5. Leave the pull request open and unmerged; do not begin SD-008 or deployment.

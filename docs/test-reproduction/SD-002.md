# SD-002 Test Reproduction

**Date:** August 17, 2026

**Branch:** `work/sd-002-prioritized-issue`

**Base:** `f79274c6b5ef2bcda77416c445f7e8905d06171f`

**Runtime:** Node.js `24.14.0`, npm `11.9.0`, Next.js `16.3.1`, Prisma `6.11.1`

## Preconditions

1. Run from the SignalDesk repository root.
2. Install exact dependencies with `npm ci`.
3. Keep Google Chrome installed at its normal Windows location.
4. Do not set a production `DATABASE_URL` for the ordered test ladder.

Integration, Workflow, and Stress create isolated real PostgreSQL clusters under ignored
`.elder/runtime/` paths, apply both committed migrations, and stop their processes on exit.

## Ordered ladder

Run each command separately and stop on failure:

```powershell
npm run test:foundation
npm run test:component
npm run test:integration
npm run test:workflow
npm run test:stress
```

Expected proof:

| Level | Expected result |
|---|---|
| Foundation | 13 tests; exact lockfile; Prisma generation and validation; TypeScript; ESLint; source layout; zero dependency-boundary violations |
| Component | 25 tests including Product Issue acknowledgement, priority, bounds, restricted content, and workflow binding |
| Integration | 8 real-PostgreSQL tests including exact issue lineage, accepted-state and revision eligibility, immutability, duplicate rejection, and concurrent one-winner promotion |
| Workflow | 2 Chrome tests at desktop and mobile widths; UI-only creation, acceptance, promotion, lineage display, reload, persistence, overflow, and browser-error checks |
| Stress | 11 checks including input bounds, confidential-log exclusion, triage contention, non-accepted and stale issue rejection, issue contention, database outage, recovery, and durable issue state |

## Build, audit, and capsule validation

```powershell
npm audit --json
npm run build
.\elder.ps1 validate
```

Expected results:

- npm audit reports zero vulnerabilities.
- Next.js produces an optimized build containing
  `/api/v1/signals/[signalId]/product-issue`.
- Elder validates all installed capsule-owned protocol files.

## Delivery readiness reproduction

```powershell
.\elder.ps1 factory status
.\elder.ps1 autonomy readiness
```

Expected current result: the product implementation remains locally runnable and ordinary GitHub
has reviewable PR `Neel-Error404/signal-desk#1`, while the capsule still reports no initialized
hosted repository integration and `can_create_pr: false`. This is an Elder integration and
authority limitation, not a failing product test or a claim that Elder created the PR.
